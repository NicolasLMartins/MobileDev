import { Pessoa } from "./types/classes/Pessoa";

const nomes: string[] = ["Nícolas", "Larah", "Eduardo", "Lucas", "Sarah"];
const idades: number[] = [17, 18, 19, 20, 21];

Pessoa.listaPessoa(nomes);
Pessoa.listaPessoa(idades);